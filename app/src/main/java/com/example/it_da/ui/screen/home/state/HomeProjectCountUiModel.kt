package com.example.it_da.ui.screen.home.state

// Represents the project count summary shown at the top of the home screen.
data class HomeProjectCountUiModel(
    val applyingCount: Int,
    val participatingCount: Int,
    val completedCount: Int
)
